/// @description Save data select

instance_create(x, y, obj_fade_in);

saving = false;

page = 0;
loadOption = 0;

data = 0;
data_menu_load(data);

cancelOption = false;

canSelect = true;

